> 本文记录旧 XTData Actor 的历史账单。当前已切换 Xquik；新输入及费用说明见 [Xquik 批量采集](XQUIK.md)。

# Apify 实网采集记录

日期：2026-09-08。本机正式服务：http://localhost:8080；演示环境保持隔离。

## 当前运行状态与费用核对

用户反馈免费额度消耗过快后，三个正式来源均已暂停自动采集，保留已入库内容。本次排查只读取 Apify 账单，没有创建新 Actor 运行。

通过每个 Run 的 `usageTotalUsd` 和 `chargedEventCounts` 核对：

| 来源 | 返回帖子 | 计费条数 | 启动事件 | 实际金额 |
| --- | ---: | ---: | ---: | ---: |
| NASA Webb | 0 | 50 | 1 | $0.2505 |
| 自然风景关键词 | 52 | 52 | 1 | $0.2605 |
| NASA Hubble | 3 | 50 | 1 | $0.2505 |
| 合计 | 55 | 152 | 3 | $0.7615 |

免费账户当前单价为 $5 / 1000 条，启动事件 $0.0005；宣传页的 $0.25 / 1000 条对应较高付费计划。上述空结果及低产出运行都实际按 50 条收费。来源每 6 小时一次意味着三个来源每天 12 次运行，仅按最低计费估算，30 天也约 $90.18。价格依据 [Actor 定价页](https://apify.com/xtdata/twitter-x-scraper/pricing)，以实际账单为准。

后台现已显示费用说明，手动采集需确认预计费用；新增来源默认暂停，界面预填每 7 天一次。尺寸设置改为标准、高清、不限尺寸与自定义，标准保持原 720 / 1280 阈值。尺寸筛选发生在抓取后，不抵扣 Apify 费用。

## 已完成配置

- 用户提供的 Token 已通过 Apify 认证验证（HTTP 200），经正式管理员接口以 AES-256-GCM 加密保存到数据库。密钥未写入本文、前端资源或仓库配置。
- 数据库存储检查确认仅保存 ciphertext；正式后台浏览器检查确认页面和配置接口不返回 Token 明文。
- 首轮 Actor 仅使用 `xtdata/twitter-x-scraper`，来源最初按每 6 小时、最多 100 条配置，首次回溯 7 天；现已暂停自动采集。全局单并发，两次启动至少间隔 5 分钟。
- 仅导入静态图片，短边至少 720 px、长边至少 1280 px；合格图片保存到本地后发布。

| 来源 | 查询 | 主题 |
| --- | --- | --- |
| NASA Webb · 深空影像 | `from:NASAWebb` | 星空宇宙 |
| NASA Hubble · 星系影像 | `from:NASAHubble` | 星空宇宙 |
| 自然风景 · 高清壁纸 | `(landscape OR mountains OR ocean) (wallpaper OR #wallpapers) -filter:replies -filter:retweets` | 自然风光 |

NASA 账号依据 [NASA 官方社交媒体目录](https://www.nasa.gov/social-media/)选择。关键词来源用于发现不同作者的风景壁纸；原作者与原帖链接按采集结果保留。

## 首轮运行

首个 NASA Webb 运行 （已省略实例 ID） 已完成，Dataset （已省略实例 ID） 返回 0 条。Actor 日志提示 `Empty page or API Error`，不能仅凭 SUCCEEDED 判断获得了图片，也不能据此确认该账号近期没有图片。

| 来源 | Run ID | 返回帖子 | 导入作品 | 最终未解决失败 |
| --- | --- | ---: | ---: | ---: |
| NASA Webb | （已省略实例 ID） | 0 | 0 | 0 |
| 自然风景关键词 | （已省略实例 ID） | 52 | 35 | 0 |
| NASA Hubble | （已省略实例 ID） | 3 | 3 | 0 |

合计 38 组作品、78 张原图。低于尺寸阈值的图片被跳过，空结果未替换成演示数据。人工下架 8 组明显不适合作为屏幕壁纸的宣传及家装内容；最终公开 30 组作品。首页已配置两个真实图片横幅及推荐作品、作者。

## 实网修复与验证

- 本机直连 `pbs.twimg.com` 超时，而已配置的 Clash HTTP 代理可连接。新增服务端 `MEDIA_PROXY_URL`，本机配置为 `http://127.0.0.1:7897`。该代理需保持运行；没有修改系统代理设置。
- 原有 5 个失败项从相同 Dataset 重试，全部解决，没有重新启动付费 Actor。服务重启后从已保存的 Dataset offset 恢复，已落盘作品未重复创建。
- 当前选中的第三张原图下载返回 HTTP 200，382109 字节，与磁盘原图 SHA-256 一致。该验收请求产生一条真实的站内下载记录。
- 新增代理测试验证：未配置时保持直连；非法代理配置被拒绝；被禁止的目标不会到达代理；仅允许的媒体域名通过 CONNECT 下载，代理失败不绕过为直连。
- Go 测试共 10 项及 `go vet ./...` 通过。集成测试仍使用隔离 schema；测试任务锁是数据库级锁，运行这部分测试时先暂停正式采集实例，避免与实际导入互斥。测试后已恢复服务。
- 正式首页在 1440、1024、390 宽度检查本地图片加载与横向溢出，并保存详情页截图。截图：`artifacts/live-home-*.png`、`live-detail-1440.png`、`live-admin-sources.png`。
